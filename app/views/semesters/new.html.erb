<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@semester, :method => 'post') do |f| %>
        <%= render(:partial => 'form', :locals =>{:f => f})%>
        <div class="action">
          <%= f.submit "Create", class:"btn btn-primary" %>
        </div>
    <% end %>
  </div>
</div>
